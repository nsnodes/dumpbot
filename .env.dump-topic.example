# Cutover target: t.me/c/3904448873/311
# The DumpBot bot must be added to this forum with privacy mode disabled so it
# receives ordinary topic messages. Do not reuse the steward bot token: two
# getUpdates pollers on one token will steal updates from each other.
TELEGRAM_BOT_TOKEN=your_dumpbot_token_here
CHAT_ID=-1003904448873
TOPIC_ID=311

# This path is intentionally outside the public Git checkout.
DATA_OUTPUT_DIR=/var/lib/dump-collector
EXPORT_MODE=private
GIT_EXPORT_ENABLED=false
